version = 1

[cli_version]
max_wall_seconds = 5.0
max_output_bytes = 128
rationale = "A generous cross-platform regression ceiling for the installed typed CLI entry point."

[map_small_project]
max_wall_seconds = 30.0
max_output_bytes = 512
rationale = "A cold CLI process must map a representative local project far below the five-minute product ceiling."

[complete_demo]
max_wall_seconds = 60.0
max_output_bytes = 4096
rationale = "The no-Atlas proof must complete within the standard bounded-check target on CI hardware."
