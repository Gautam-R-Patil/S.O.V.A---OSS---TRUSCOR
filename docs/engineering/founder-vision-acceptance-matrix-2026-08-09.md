<!-- status: experiment -->

# Founder-vision acceptance matrix — updated 2026-08-20

This is the controlling gap ledger for the requested whole-product validation.
“Implemented” means a real acceptance path exists; it never means universal,
secure, or independently validated beyond the stated boundary.

| Founder capability | Current evidence | State | Exit criterion |
|---|---|---|---|
| Authorized website execution | `arena explore-web` gives a provider or attested OCI planner successive secret-redacted accessibility snapshots and lets it propose typed plans across navigation, history, forms, pointer/keyboard, drag, modal, and bounded-tab workflows. Every observable boundary is recorded as an entry in a signed per-batch trace, invalid plans are rejected and safely replanned, deterministic success must reproduce from reset, and installed Chrome has passed the strengthened multi-page/video/replay lane. Allowed-origin request filtering and post-action drift detection do not claim a process-network or VM egress sandbox | Working, bounded; all-nine static preflight passed, provider run quota-blocked | The prepared nine-challenge AI Goat provider campaign passes, including Knowledge Base Challenges 3, 7, and 8, with verified capsules and exact-moment replay |
| Consequential UI workflows | The resettable browser action lab visibly creates a disposable file, transfers mail through loopback SMTP, posts a message to a loopback sink, changes a disposable setting, independently verifies every effect, reproduces them, and packages video, signed traces, replay, and a registry-ready module | Working contained vertical | New real service families require their own declared adapters, substitute accounts, proof of control, effect sensors, and exact authorization; this fixture is not authority over arbitrary real accounts |
| Arbitrary desktop-software UI testing | Exact CUA release, service owner, adapter contract, unit tests, and live read tools exist; live mutation could not be independently verified on this runner | External runner blocked | Fixture-owned click/type/save passes with exact HWND, independent effect check, cancellation, timeout, and recovery on each supported OS |
| Strong untrusted-agent isolation | The external-agent adapter accepts only exact digest-pinned images through a ready, host-attested gVisor `runsc` runtime. It enforces no network, credentials, host mounts, engine socket, capabilities, or writable root; runs non-root with CPU/memory/PID/time/output bounds; passes requests over bounded stdin rather than the host command line; verifies cleanup; and refuses fallback to ordinary OCI | Implemented and mandatory-test validated; live `runsc` host unavailable here | A production operator registers and patches `runsc`, builds an independent agent image, passes the optional live conformance/cleanup lane, and records external host-security review; gVisor is a user kernel, not a separate VM kernel |
| Arbitrary third-party agents | `sova agent conform-oci`, `arena agent-run`, and `arena explore-web` accept a strict `describe`/`self-test`/`respond` JSON protocol through the isolated OCI adapter. Agents return messages or typed plans and never receive raw browser, target, host, or approval authority | Working contract; live third-party image pending | Independently built digest-pinned agents pass signed conformance and controlled Arena experiments on a registered `runsc` host |
| Persistent authenticated browser sessions | Opaque target-bound profile vault, cross-process exclusive leases, bounded stale recovery, manual headful login/CAPTCHA handoff, campaign integration, graceful shutdown, and an installed-Chrome two-process persistence proof pass without profile handle/path/cookie material entering traces or reports | Working, bounded | Founder-selected control-proven site login is completed manually, survives restart, and passes an authorized campaign without scope drift; site-specific compatibility remains external |
| Broad computer-use execution | MELRA capability probe and CUA read lane pass; mutation is not admitted by default | Working, bounded | At least two independently implemented adapters pass the same fixture-owned action/effect conformance suite |
| Multi-agent swarms | Arena Chamber supports bounded agent/environment, agent/agent, and multi-agent topologies. The browser-swarm lane now runs multiple roles over one target-bound opaque profile with strict grants, per-agent/global budgets, cancellation, fresh browser approval, redacted shared observations, signed coordinator/subtraces, live/final parity, and an installed-Chrome self-owned-fixture proof | Working, bounded | Founder-selected control-proven site and optional real-provider roles pass without scope/session drift; desktop topology remains adapter-gated |
| Trigger hunting | Fixed campaigns, multi-round adaptive campaigns, and semantic multi-page exploration record failures and near misses. A clean persisted-oracle pass is freshly reproduced before SOVA emits a discovery capsule with decisive media cues; failed or not-observed attempts remain explicit evidence and do not imply decisive cues | Working, bounded; final OpenRouter probe rate-limited and campaign not launched | Prepared AI Goat all-nine provider lane passes, then adaptive search is benchmarked against declared baselines on held-out authorized fixtures |
| Provider orchestration | Tool-isolated roles, strict schemas, token/cost budgets, ordered same-provider fallbacks, bounded transient retry, optional Codex/provider/OCI lanes, and bounded recovery from malformed, denied, over-budget, or over-batched plans exist. Rejected plans never execute and consecutive rejections terminate the mission. The final OpenRouter all-nine availability probe surfaced account-wide free-tier rate exhaustion and prevented campaign launch rather than fabricating a result | Working, bounded; provider availability gate open | Two real providers and one independent OCI planner pass the same blinded safe suite; cost/rate exhaustion remains an explicit result |
| Forensics and counterfactuals | Evidence-linked partial order, paired browser interventions, uncertainty, and abstention exist | Working, bounded | Randomized/blinded ground-truth study estimates calibration, false attribution, and coverage |
| Rehearsal and composition | Sanitized substitute workspaces, selective promotion, bounded composition search, and signed evidence exist | Working, bounded | Comparative real-system studies establish when methods outperform named baselines |
| Mapping, monitoring, extensions, probe | Offline/authorized workflows exist; monitoring now includes a foreground scheduler with durable state, bounded history/artifact retention, kernel overlap exclusion, restart recovery, cancellation, and signed drift-result evidence | Working, bounded | Service-manager deployment and end-to-end external alert acknowledgement remain operator integrations |
| Live registry and leaderboard | A self-hostable reference service provides token-gated staged uploads, an asynchronous verification queue, pinned evidence signers, atomic content-address promotion, a signed service index with client trust/sequence checks, SSE updates, abuse limits, restart recovery, standard-profile-only leaderboard rows, and an exact health/key-digest contract. A digest-gated non-root/read-only/capability-dropped registry plus Caddy TLS blueprint and mandatory preflight ship in `deploy/community` | Blueprint and preflight test-validated; not publicly or production deployed | A named operator deploys the blueprint with identity, moderation, backup/restore, key rotation, monitoring, DDoS/abuse controls, incident response, and independent security review |
| Continuous monitoring service | `sova monitor serve` passes durable state, kernel-backed overlap prevention, interrupted-run recovery, cancellation, report/history retention, and signed alert evidence over exact operator-supplied snapshots | Working, bounded | Remote collection, service supervision, high availability, and external notification acknowledgement remain explicit adapters |
| Cinematic and exact-moment replay | The evidence-native application provides play/pause, 0.5x-8x navigation, scrubbing, synchronized sensor-family lanes, search, recorded causal/correlation links, comparison, hostile-content-safe details, sealed/live-prefix distinction, and integrity-checked loopback SSE tailing. `sova replay FINDING.sova` verifies and opens generated local HTML at the duration-bounded trace event where the deterministic oracle passed; `--no-open` renders it for automation. The explicit `replay capsule` form remains compatible | Working, bounded | The all-nine AI Goat campaign verifies each successful capsule, cue, media digest, decisive frame, and comparison trace; large-trace usability/accessibility review remains external |
| Independent causal validation | A strict three-phase blinded protocol separates label-free tasks, frozen predictions, and commitment-checked scoring; schemas, stochastic nondeterministic fixtures, Wilson intervals, abstention metrics, DSSE reviewer-key pins, tamper rejection, and predeclared gates pass | Working protocol; external study pending | A separately governed reviewer and real nondeterministic ground-truth dataset pass predeclared thresholds with intervention-fidelity review; a key signature alone never proves reviewer identity or independence |
| Community deployment | Signed local/offline registry, staging/verification service, health contract, and hardened TLS deployment blueprint with mandatory immutable-image/operator-secret preflight exist | Blueprint/preflight test-validated; not hosted or production deployed | Operator completes the production controls listed above and independently security-reviews a public service; SOVA never silently uploads |

## Non-negotiable bounds

- CAPTCHA bypass, automated account creation, credential harvesting, and testing
  outside exact ownership/control proof are not completion criteria.
- “Unrestricted swarm” is replaced by operator-bounded concurrency, actions,
  origins, time, output, cost, and effects.
- Observable requests, responses, tool actions, approvals, effects, artifacts,
  and state transitions are recorded. Hidden model thoughts are not claimed.
- Provider observations and hash chains provide bounded provenance and tamper
  evidence, not unforgeability, non-repudiation, truth, or independent audit.
- External review, a public deployment, a founder-selected production target,
  and unavailable host isolation features remain external gates rather than
  percentages disguised as completed engineering.
